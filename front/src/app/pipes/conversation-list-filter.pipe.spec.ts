/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ConversationListFilterPipe } from './conversation-list-filter.pipe';

describe('Pipe: ConversationListFilter', () => {
  it('create an instance', () => {
    let pipe = new ConversationListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
